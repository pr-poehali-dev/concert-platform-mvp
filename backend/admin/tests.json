{
  "tests": [
    {
      "name": "Login wrong password",
      "method": "POST",
      "path": "/?action=login",
      "body": { "password": "wrong" },
      "expectedStatus": 401,
      "expectedBody": { "error": "string" },
      "bodyMatcher": "partial"
    },
    {
      "name": "Stats without token",
      "method": "GET",
      "path": "/?action=stats",
      "expectedStatus": 403,
      "expectedBody": { "error": "string" },
      "bodyMatcher": "partial"
    },
    {
      "name": "Users without token",
      "method": "GET",
      "path": "/?action=users",
      "expectedStatus": 403,
      "expectedBody": { "error": "string" },
      "bodyMatcher": "partial"
    }
  ]
}
